
/**
 * Used for account type
 */
export const AccountType = Object.freeze({
    MANAGEMENT: 'management',
    DELIVERY: 'delivery'
});

/*
 * Express Session information
 *
 * express.session.isLoggedIn
 * express.session.username
 * express.session.accountType
 */
